export const metadata = {
  title: 'Sign In',
  description: 'Sign in to your Field & Co account.',
  robots: { index: false, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
