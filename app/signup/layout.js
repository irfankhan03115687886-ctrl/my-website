export const metadata = {
  title: 'Create Account',
  description: 'Create a Field & Co account to track orders and save your wishlist.',
  robots: { index: false, follow: true },
};

export default function SignupLayout({ children }) {
  return children;
}
