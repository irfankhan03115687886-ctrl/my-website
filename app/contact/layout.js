export const metadata = {
  title: 'Contact',
  description: 'Get in touch with the Field & Co team about orders, sizing, or repairs.',
};

export default function ContactLayout({ children }) {
  return children;
}
